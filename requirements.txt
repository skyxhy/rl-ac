# Python 依赖（供 environment.yml 的 pip 段与 pip install -r 使用）
# 版本为大版本约束，实测通过组合见 environment.yml；torch/torch-geometric 版本敏感，
# 若遇算子不兼容请以 environment.yml 的锁定版本为准。
numpy>=1.26,<2
pandas>=2.0,<3
pyyaml>=6.0
matplotlib>=3.7
scikit-learn>=1.3
xgboost>=2.0
joblib>=1.3
tqdm>=4.65
torch>=2.1,<2.3
torch-geometric>=2.5
# 注：VeReMi 轨迹解析需要 Go 工具链（test-go/main.go），不属于 pip 依赖。
